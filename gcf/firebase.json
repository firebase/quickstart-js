{
  "firebase": "<APP_ID>",
  "public": "./",
  "ignore": [
    "firebase.json",
    "**/.*",
    "**/node_modules/**",
    "functions"
  ],
  // [START trigger_rules]
  "rules": "security-rules.json",
  "functions": {
    // Directory must contain a index.js file that exports the Google Cloud Functions
    ".source": "functions",
    "makeuppercase": { // Must match a module export in your Node.js code
      "triggers": {
        "database": {
          "path": "/messages/$message", // Path of database objects that trigger the function
          "condition": "!data.exists()" // Only run once on new messages only
        }
      }
    }
  }
  // [END trigger_rules]
}
