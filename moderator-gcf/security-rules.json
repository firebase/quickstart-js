{
  "rules": {
    "messages": {
      "$message": {
        // Users can only add new messages to the review queue.
        ".write": "!data.exists()"
      }
    },
    "sanitized": {
      // Users can read sanitized messages.
      ".read": "true"
    }
  }
}
