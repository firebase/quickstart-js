{
  "name": "scripts",
  "version": "1.0.0",
  "description": "A script to send mock data to the prod firebase console",
  "main": "MockRestaurantData.js",
  "scripts": {
    "populate-emulators": "cp ../environments/environment.ts ./environment.js && node PopulateFirestore.js",
    "populate-production": "cp ../environments/environment.prod.ts ./environment.js && node PopulateFirestore.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "Apache-2.0",
  "type": "module"
}
