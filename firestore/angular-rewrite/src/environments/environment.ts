/**  If connecting to a live Firebase project (one that you set up in the 
 * Firebase console) put your config vars here. Otherwise, leave blank to 
 * enable connection to demo project.
*/
export const environment = {
    firebase: {
        projectId: 'demo-friendly-eats',
        apiKey: 'random'
    }
}